import { addToCart, removeFromCart } from "./reducers/Cart.slice";
import { addToWhishlist, removeFromWhishlist } from "./reducers/Whishlist.slice";

export {
    addToCart, 
    removeFromCart,
    addToWhishlist, 
    removeFromWhishlist 
}